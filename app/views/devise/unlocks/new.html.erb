<!--Not mandated by any functional requirements-->

<div class="flex justify-center items-center">
  <div class="card w-full max-w-xl">
    <h1 class="heading-1">Resend unlock instructions</h1>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, "data-turbo" => false }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <div class="flex flex-col space-y-4">
        <div class="field">
          <%= f.label :email, class: 'label' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'text-field' %>
        </div>

        <%= f.submit "Resend unlock instructions", class: 'primary-button' %>
      </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</div>
