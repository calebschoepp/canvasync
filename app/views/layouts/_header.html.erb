<header class="w-full bg-neutral-50 drop-shadow-md p-4 flex sticky top-0 z-50">
  <div class="grow">
    <%= link_to '/' do %>
<!--      TODO: Make this a logo image asset-->
      <h1>Canvasync</h1>
    <% end %>
  </div>

  <% if user_signed_in? %>
    <%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete } %>
  <% end %>
</header>